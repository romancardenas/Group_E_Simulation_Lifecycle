#include <stdio.h>

/**
 * Simulation Lifecycle: main function. So far, it just prints "Hello, World!"
 * @param argc number of arguments
 * @param argv value of the arguments
 * @return always 0
 */
int main(int argc, char *argv[]) {
    puts("Hello, World!");
    return 0;
}